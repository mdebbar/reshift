require('../test-helpers/expect-code-equality')
const { reShift } = require('..')

test('remove unused variables', () => {

})

test('split declarators into their own declaration statements', () => {

})
